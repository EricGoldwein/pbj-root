# Normalize line endings to LF so Git doesn't show false "modified" on Windows
# when the editor uses LF and core.autocrlf is true.
* text=auto eol=lf
# Large combined dumps stay local only (see .gitignore). Production uses ProviderInfoNorm snapshots.
